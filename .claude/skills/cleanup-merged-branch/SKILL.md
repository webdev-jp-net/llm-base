---
name: cleanup-merged-branch
description: developへマージ済みの、開発で使ったローカルfeatureブランチを後始末するスキル。「マージしたブランチを削除」「featureブランチの後始末」「developに戻ってブランチを消す」「役割を終えたブランチを片付ける」などのリクエストで使用。統合先（develop）へ移動・最新化し、リモートに同名ブランチが無いことを確認したうえでマージ済みのローカルブランチを安全に削除する。
---

# マージ済みブランチの後始末

PRが`develop`へマージされ、役割を終えたローカルfeatureブランチを安全に片付ける。
削除は破壊的操作になりうるため、**マージ済みのみを許す安全削除を基本**とし、強制削除（`-D`）は承認を得てから行う。

## 前提と方針

- 統合先ブランチは既定で`develop`（フローが異なる場合はマージ先に読み替える）。
- 変更を加えるのは**ローカルブランチのみ**（リモートは確認のため読み取るだけ）。
- 破壊的操作（`-D`強制削除）は**実行前に承認を取る**。

## ワークフロー

### Step 1: 現在の状況確認

```bash
git branch --show-current
git status --short
```

- 現在のブランチが`develop`／`main`の場合は、後始末対象のfeatureブランチがどれか不明。**どのブランチを削除するかユーザーに確認**する。
- featureブランチ上にいる場合、それが後始末対象。未コミットの変更が残っていれば、削除前に扱い（コミット要否）を確認する。

### Step 2: 統合先（develop）へ移動

削除対象のブランチ上からは自身を削除できないため、先に`develop`へ移動する。

```bash
git checkout develop
```

### Step 3: developをマージ済みリモート状態へ更新

ローカル`develop`が古いと、Step 5の安全削除が「未マージ」と誤判定される。`--ff-only`で履歴を壊さず最新化する。

```bash
git pull --ff-only
```

### Step 4: リモートに同名ブランチが無いことを確認

```bash
git ls-remote --heads origin <feature-branch>
```

- **同名ブランチが無い** → ローカル削除OK。Step 5へ進む。
- **同名ブランチが有る** → ローカル削除しない。リモートにまだ同名ブランチが残っている旨を報告して停止する（後始末の前提が整っていない）。

### Step 5: ローカルfeatureブランチを安全削除

```bash
git branch -d <feature-branch>
```

- `-d`は**マージ済みのブランチだけ**を削除する安全オプション。成功すればStep 6へ進む。
- **失敗した場合**（"not fully merged"等）は、squash／rebaseマージでコミットが`develop`履歴に同一の形で残っていないことが主因。その理由をユーザーに説明し、**本当に破棄してよいか確認してから**`git branch -D <feature-branch>`（強制削除）を実行する。勝手に`-D`しない。`-D`が成功した場合もStep 6へ進む。

### Step 6: 記録の更新

削除したブランチでの作業について、`_llm-memories/`直下の`state.md`・`lessons.md`・`episodes.md`を更新する（`_llm-rules/self_improvement.md`のMemory運用ルール）。更新前にこの3ファイルを読む。

- `state`: `branch`が削除したブランチと一致し、かつ未完了のフェーズと`pending_items`が残っていない場合のみ`completed`にする。未完了が残っていれば触らず、何が残っているかを報告する。`completed`にするときは現在フェーズ・チェックポイント・引き継ぎメモも整合させる
- `episodes`: 削除したブランチでの作業を表す行が概要欄に無ければ1行追加する。同じIssueの別タスクは別行として扱う
- `lessons`: 失敗や手戻りが記録に残っていれば教訓を追加。既存教訓の再発なら発生回数を増やす

更新は`Edit`で変更箇所だけを置換する。別セッションでの後始末では、失敗や試行錯誤はコミット履歴に残っていない。記録にない事柄を推測で書かず、書けない旨を報告する。

記録の更新に失敗しても、ブランチの削除は巻き戻さない（削除は既に成立している）。更新できなかったファイル名と追記すべき内容を報告し、Step 7へ進む。

### Step 7: 結果の報告

```bash
git branch
git log --oneline -1
```

削除したブランチ名・現在地（`develop`）・残ったローカルブランチ・更新した記録を報告する。

## 注意

- `develop`以外（例: `main`直マージ運用）の場合は、Step 2・3のブランチ名を読み替える。
- このスキルはgit操作を実行するが、**安全削除（`-d`）以外の破壊的操作は必ず承認を得る**。
