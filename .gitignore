# OS / editor
.DS_Store

# Dependencies
node_modules/

# claude
.claude/tmp
.claude/settings.local.json
.claude/launch.json
.claude/worktrees

# serena (ツールが自動生成・自動更新するため追跡しない)
.serena/

# playwright-mcp (実行時の生成物)
.playwright-mcp/

# エージェントの実行記録（テンプレートのみ追跡し、記録の実体は追跡しない）
_llm-memories/*
!_llm-memories/templates/
